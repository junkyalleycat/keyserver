#!/usr/bin/env python3.7

import asyncio
import os
import sys

libpath = "%s/../lib" % os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, libpath)

from keyserver.server import main

sys.exit(asyncio.run(main()))
